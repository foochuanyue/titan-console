import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'division_console',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
